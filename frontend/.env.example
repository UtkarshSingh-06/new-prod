# Copy to frontend/.env.local for local overrides.

# Dev: omit this to use Vite proxy (/api → http://127.0.0.1:8000). Set only if the API
# is elsewhere (e.g. Docker host, staging).
# VITE_API_BASE=http://127.0.0.1:8000
